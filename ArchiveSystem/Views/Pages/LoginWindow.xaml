<Window x:Class="ArchiveSystem.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="وثاق"
        WindowState="Maximized"
        WindowStyle="SingleBorderWindow"
        ResizeMode="CanResize"
        MinHeight="600" MinWidth="900"
        WindowStartupLocation="CenterScreen"
        FlowDirection="RightToLeft"
        FontFamily="Noto Kufi Arabic, Segoe UI"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        Background="#E8EAED"
        Icon="pack://application:,,,/Assets/app.ico">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="420" MinWidth="380" MaxWidth="520"/>
        </Grid.ColumnDefinitions>

        <!-- ══ LOGIN CARD ══ -->
        <Grid Grid.Column="1" Background="#F0F4F2">
            <Border VerticalAlignment="Center"
                    HorizontalAlignment="Center"
                    Width="340"
                    Background="White"
                    CornerRadius="18"
                    Padding="36,40,36,28">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="48" ShadowDepth="0"
                                      Color="#000000" Opacity="0.12"/>
                </Border.Effect>

                <StackPanel>

                    <TextBlock Text="تسجيل الدخول"
                               FontSize="22" FontWeight="Bold"
                               Foreground="#0D3B2B"/>

                    <TextBlock x:Name="SubtitleText"
                               Text="أدخل بيانات حسابك للدخول إلى النظام."
                               FontSize="12" Foreground="#888"
                               TextWrapping="Wrap"
                               Margin="0,6,0,20"/>

                    <!-- Error Banner -->
                    <Border x:Name="ErrorBorder"
                            Background="#FFF0F0" BorderBrush="#FFBCBC"
                            BorderThickness="1" CornerRadius="8"
                            Padding="12,8" Margin="0,0,0,16"
                            Visibility="Collapsed">
                        <TextBlock x:Name="ErrorText"
                                   Foreground="#C62828" FontSize="12"
                                   TextWrapping="Wrap"/>
                    </Border>

                    <!-- First-run Setup Banner -->
                    <Border x:Name="SetupBorder"
                            Background="#E8F5E9" BorderBrush="#A5D6A7"
                            BorderThickness="1" CornerRadius="8"
                            Padding="12,8" Margin="0,0,0,16"
                            Visibility="Collapsed">
                        <TextBlock Text="مرحباً! أول تشغيل للنظام. يرجى إنشاء حساب مدير الأرشيف."
                                   Foreground="#2E7D32" FontSize="12"
                                   TextWrapping="Wrap"/>
                    </Border>

                    <!-- Full Name (first run only) -->
                    <TextBox x:Name="FullNameBox"
                             materialDesign:HintAssist.Hint="الاسم الكامل"
                             Style="{StaticResource MaterialDesignOutlinedTextBox}"
                             FontSize="14" Margin="0,0,0,14"
                             Visibility="Collapsed"/>

                    <!-- Username -->
                    <TextBlock Text="اسم المستخدم"
                               FontSize="12" Foreground="#555"
                               Margin="0,0,0,4"/>
                    <TextBox x:Name="UsernameBox"
                             materialDesign:HintAssist.Hint=""
                             Style="{StaticResource MaterialDesignOutlinedTextBox}"
                             FontSize="14" Margin="0,0,0,16"
                             KeyDown="Input_KeyDown"/>

                    <!-- Password -->
                    <TextBlock Text="كلمة المرور"
                               FontSize="12" Foreground="#555"
                               Margin="0,0,0,4"/>
                    <Grid Margin="0,0,0,28">
                        <PasswordBox x:Name="PasswordBox"
                                     materialDesign:HintAssist.Hint=""
                                     Style="{StaticResource MaterialDesignOutlinedPasswordBox}"
                                     FontSize="14" Padding="12,0,44,0"
                                     KeyDown="Input_KeyDown"/>
                        <TextBox x:Name="PasswordVisibleBox"
                                 materialDesign:HintAssist.Hint=""
                                 Style="{StaticResource MaterialDesignOutlinedTextBox}"
                                 FontSize="14" Padding="12,0,44,0"
                                 VerticalContentAlignment="Center"
                                 Visibility="Collapsed"
                                 KeyDown="Input_KeyDown"/>
                        <Button x:Name="TogglePasswordButton"
                                Width="36" Height="55" Padding="0"
                                FontSize="15" Content="👁"
                                Background="Transparent" BorderThickness="0"
                                Cursor="Hand"
                                VerticalAlignment="Center"
                                HorizontalAlignment="Left"
                                Margin="232,0,0,0"
                                Click="TogglePasswordButton_Click"/>
                    </Grid>

                    <!-- Login Button -->
                    <Button x:Name="LoginButton"
                            Content="دخول إلى النظام"
                            FontSize="15" FontWeight="SemiBold"
                            Height="48" BorderThickness="0" Cursor="Hand"
                            Click="LoginButton_Click">
                        <Button.Background>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                <GradientStop Color="#0D6B47" Offset="0"/>
                                <GradientStop Color="#1a9e6e" Offset="1"/>
                            </LinearGradientBrush>
                        </Button.Background>
                        <Button.Foreground>
                            <SolidColorBrush Color="White"/>
                        </Button.Foreground>
                        <Button.Style>
                            <Style TargetType="{x:Type Button}"
                                   BasedOn="{StaticResource MaterialDesignRaisedButton}">
                                <Setter Property="materialDesign:ButtonAssist.CornerRadius" Value="10"/>
                            </Style>
                        </Button.Style>
                    </Button>

                    <!-- ── RESTORE SEPARATOR ── -->
                    <Border BorderThickness="0,1,0,0"
                            BorderBrush="#F0F0F0"
                            Margin="0,20,0,0"
                            Padding="0,16,0,0">
                        <StackPanel>
                            <TextBlock Text="نقل البيانات من جهاز آخر؟"
                                       FontSize="11" Foreground="#999"
                                       HorizontalAlignment="Center"
                                       Margin="0,0,0,8"/>

                            <Button x:Name="RestoreDbBtn"
                                    Content="📂  استعادة قاعدة البيانات"
                                    Style="{StaticResource MaterialDesignOutlinedButton}"
                                    Height="36"
                                    FontSize="12"
                                    HorizontalAlignment="Stretch"
                                    Foreground="#555"
                                    BorderBrush="#CCCCCC"
                                    Click="RestoreDb_Click"/>
                        </StackPanel>
                    </Border>

                </StackPanel>
            </Border>
        </Grid>

        <!-- ══ HERO PANEL ══ -->
        <Grid Grid.Column="0" Background="#E8EAED">
            <Border CornerRadius="20" Margin="32,32,32,32" ClipToBounds="True">
                <Border.Background>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                        <GradientStop Color="#042B1A" Offset="0"/>
                        <GradientStop Color="#0A4D2E" Offset="0.30"/>
                        <GradientStop Color="#0D6B45" Offset="0.60"/>
                        <GradientStop Color="#0A3D2A" Offset="0.80"/>
                        <GradientStop Color="#041E12" Offset="1"/>
                    </LinearGradientBrush>
                </Border.Background>

                <Grid>

                    <!-- Decorative circles — gold tones on deep green -->
                    <Ellipse Width="560" Height="560"
                             HorizontalAlignment="Right" VerticalAlignment="Top"
                             Margin="0,-220,-220,0" Opacity="0.09">
                        <Ellipse.Fill>
                            <RadialGradientBrush>
                                <GradientStop Color="#D4AF37" Offset="0"/>
                                <GradientStop Color="Transparent" Offset="1"/>
                            </RadialGradientBrush>
                        </Ellipse.Fill>
                    </Ellipse>

                    <Ellipse Width="420" Height="420"
                             HorizontalAlignment="Left" VerticalAlignment="Bottom"
                             Margin="-180,0,0,-180" Opacity="0.10">
                        <Ellipse.Fill>
                            <RadialGradientBrush>
                                <GradientStop Color="#1adf8a" Offset="0"/>
                                <GradientStop Color="Transparent" Offset="1"/>
                            </RadialGradientBrush>
                        </Ellipse.Fill>
                    </Ellipse>

                    <!-- Soft centre glow -->
                    <Ellipse Width="340" Height="340"
                             HorizontalAlignment="Center" VerticalAlignment="Center"
                             Margin="0,60,0,0" Opacity="0.07">
                        <Ellipse.Fill>
                            <RadialGradientBrush>
                                <GradientStop Color="#D4AF37" Offset="0"/>
                                <GradientStop Color="Transparent" Offset="1"/>
                            </RadialGradientBrush>
                        </Ellipse.Fill>
                    </Ellipse>

                    <!-- Gold accent left edge bar -->
                    <Border Width="4" HorizontalAlignment="Left"
                            VerticalAlignment="Stretch"
                            Margin="0,60,0,60" CornerRadius="2">
                        <Border.Background>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                <GradientStop Color="Transparent" Offset="0"/>
                                <GradientStop Color="#D4AF37" Offset="0.5"/>
                                <GradientStop Color="Transparent" Offset="1"/>
                            </LinearGradientBrush>
                        </Border.Background>
                    </Border>

                    <!-- Main content -->
                    <StackPanel VerticalAlignment="Center"
                                HorizontalAlignment="Center"
                                Margin="60,0,60,120">

                        <!-- App logo — centred, larger, with gold glow -->
                        <Image Source="pack://application:,,,/Assets/main.png"
                               Height="250"
                               HorizontalAlignment="Center"
                               Margin="0,0,0,15"
                               RenderOptions.BitmapScalingMode="HighQuality">
                            <Image.Effect>
                                <DropShadowEffect BlurRadius="40" ShadowDepth="0"
                                                  Color="#D4AF37" Opacity="0.35"/>
                            </Image.Effect>
                        </Image>

                        <!-- Title -->
                        <TextBlock Text="وثاق"
                                   FontSize="52" FontWeight="Bold"
                                   HorizontalAlignment="Center"
                                   TextAlignment="Center">
                            <TextBlock.Foreground>
                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                    <GradientStop Color="#FFFFFF" Offset="0"/>
                                    <GradientStop Color="#D4AF37" Offset="1"/>
                                </LinearGradientBrush>
                            </TextBlock.Foreground>
                            <TextBlock.Effect>
                                <DropShadowEffect BlurRadius="30" ShadowDepth="0"
                                                  Color="#000" Opacity="0.30"/>
                            </TextBlock.Effect>
                        </TextBlock>

                        <!-- Gold divider -->
                        <Border Height="2" CornerRadius="1"
                                Margin="0,12,0,16" Width="260"
                                HorizontalAlignment="Center">
                            <Border.Background>
                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                    <GradientStop Color="Transparent" Offset="0"/>
                                    <GradientStop Color="#D4AF37" Offset="0.5"/>
                                    <GradientStop Color="Transparent" Offset="1"/>
                                </LinearGradientBrush>
                            </Border.Background>
                        </Border>

                        <TextBlock Text="بحث سريع، إدخال ذكي، تقارير، وتتبع كامل لمكان كل دوسية."
                                   FontSize="15" Foreground="#A8D8C0"
                                   TextWrapping="Wrap" MaxWidth="500"
                                   LineHeight="26" TextAlignment="Center"
                                   HorizontalAlignment="Center"/>
                    </StackPanel>

                    <!-- Bottom Stats Bar -->
                    <Grid VerticalAlignment="Bottom" Margin="36,0,36,36">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <!-- Stat 1 — Last Backup -->
                        <Border Grid.Column="0" CornerRadius="12"
                                Padding="20,16" Margin="0,0,8,0">
                            <Border.Background>
                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                    <GradientStop Color="#0D6B4530" Offset="0"/>
                                    <GradientStop Color="#00000025" Offset="1"/>
                                </LinearGradientBrush>
                            </Border.Background>
                            <Border.BorderBrush>
                                <SolidColorBrush Color="#D4AF37" Opacity="0.30"/>
                            </Border.BorderBrush>
                            <Border.BorderThickness>1</Border.BorderThickness>
                            <StackPanel HorizontalAlignment="Right">
                                <TextBlock x:Name="LastBackupTimeText" Text="--:--"
                                           FontSize="24" FontWeight="Bold" Foreground="White"/>
                                <TextBlock Text="آخر نسخة احتياطية"
                                           FontSize="12" Foreground="#7EC8A0"
                                           HorizontalAlignment="Right" Margin="0,2,0,0"/>
                            </StackPanel>
                        </Border>

                        <!-- Stat 2 — Total Files -->
                        <Border Grid.Column="1" CornerRadius="12"
                                Padding="20,16" Margin="8,0,8,0">
                            <Border.Background>
                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                    <GradientStop Color="#1a9e6e28" Offset="0"/>
                                    <GradientStop Color="#00000025" Offset="1"/>
                                </LinearGradientBrush>
                            </Border.Background>
                            <Border.BorderBrush>
                                <SolidColorBrush Color="#D4AF37" Opacity="0.30"/>
                            </Border.BorderBrush>
                            <Border.BorderThickness>1</Border.BorderThickness>
                            <StackPanel HorizontalAlignment="Right">
                                <TextBlock x:Name="TotalFilesText" Text="0"
                                           FontSize="24" FontWeight="Bold" Foreground="White"/>
                                <TextBlock Text="ملف داخل النظام"
                                           FontSize="12" Foreground="#7EC8A0"
                                           HorizontalAlignment="Right" Margin="0,2,0,0"/>
                            </StackPanel>
                        </Border>

                        <!-- Stat 3 — Total Folders -->
                        <Border Grid.Column="2" CornerRadius="12"
                                Padding="20,16" Margin="8,0,0,0">
                            <Border.Background>
                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                    <GradientStop Color="#1a9e6e28" Offset="0"/>
                                    <GradientStop Color="#00000025" Offset="1"/>
                                </LinearGradientBrush>
                            </Border.Background>
                            <Border.BorderBrush>
                                <SolidColorBrush Color="#D4AF37" Opacity="0.40"/>
                            </Border.BorderBrush>
                            <Border.BorderThickness>1</Border.BorderThickness>
                            <StackPanel HorizontalAlignment="Right">
                                <TextBlock x:Name="TotalFoldersText" Text="0"
                                           FontSize="24" FontWeight="Bold">
                                    <TextBlock.Foreground>
                                        <SolidColorBrush Color="#D4AF37"/>
                                    </TextBlock.Foreground>
                                </TextBlock>
                                <TextBlock Text="دوسية مستوردة"
                                           FontSize="12" Foreground="#7EC8A0"
                                           HorizontalAlignment="Right" Margin="0,2,0,0"/>
                            </StackPanel>
                        </Border>

                    </Grid>
                </Grid>
            </Border>
        </Grid>

    </Grid>
</Window>
