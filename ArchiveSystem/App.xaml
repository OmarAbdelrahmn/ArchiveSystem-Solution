<Application x:Class="ArchiveSystem.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <materialDesign:BundledTheme BaseTheme="Light"
                                             PrimaryColor="Teal"
                                             SecondaryColor="Teal"/>
                <ResourceDictionary Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/MaterialDesign3.Defaults.xaml"/>
            </ResourceDictionary.MergedDictionaries>

            <!-- ═══════════════════════════════════════════
                 BRAND COLOR PALETTE
            ═══════════════════════════════════════════ -->
            <SolidColorBrush x:Key="PrimaryDark"     Color="#155a44"/>
            <SolidColorBrush x:Key="Primary"         Color="#1a7a60"/>
            <SolidColorBrush x:Key="PrimaryLight"    Color="#23a07f"/>
            <SolidColorBrush x:Key="PrimaryGhost"    Color="#e6f4f0"/>
            <SolidColorBrush x:Key="AccentGold"      Color="#c8932a"/>
            <SolidColorBrush x:Key="SurfaceWhite"    Color="#ffffff"/>
            <SolidColorBrush x:Key="SurfaceGrey"     Color="#f5f7fa"/>
            <SolidColorBrush x:Key="BorderLight"     Color="#dde3ea"/>
            <SolidColorBrush x:Key="TextPrimary"     Color="#1a2e25"/>
            <SolidColorBrush x:Key="TextSecondary"   Color="#4a6355"/>
            <SolidColorBrush x:Key="TextMuted"       Color="#8aa89a"/>
            <SolidColorBrush x:Key="DangerRed"       Color="#c0392b"/>

            <!-- ═══════════════════════════════════════════
                 GLOBAL FONT OVERRIDES  (Arabic-first)
            ═══════════════════════════════════════════ -->
            <Style TargetType="TextBlock">
                <Setter Property="FontFamily" Value="Noto Kufi Arabic, Segoe UI"/>
            </Style>
            <Style TargetType="TextBox" BasedOn="{StaticResource {x:Type TextBox}}">
                <Setter Property="FontFamily"     Value="Noto Kufi Arabic, Segoe UI"/>
                <Setter Property="FlowDirection"  Value="RightToLeft"/>
            </Style>
            <Style TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
                <Setter Property="FontFamily" Value="Noto Kufi Arabic, Segoe UI"/>
            </Style>
            <Style TargetType="ComboBox" BasedOn="{StaticResource {x:Type ComboBox}}">
                <Setter Property="FontFamily"    Value="Noto Kufi Arabic, Segoe UI"/>
                <Setter Property="FlowDirection" Value="RightToLeft"/>
            </Style>
            <Style TargetType="CheckBox" BasedOn="{StaticResource {x:Type CheckBox}}">
                <Setter Property="FontFamily" Value="Noto Kufi Arabic, Segoe UI"/>
            </Style>
            <Style TargetType="TabItem" BasedOn="{StaticResource {x:Type TabItem}}">
                <Setter Property="FontFamily" Value="Noto Kufi Arabic, Segoe UI"/>
            </Style>
            <Style TargetType="ListBox" BasedOn="{StaticResource {x:Type ListBox}}">
                <Setter Property="FontFamily"    Value="Noto Kufi Arabic, Segoe UI"/>
                <Setter Property="FlowDirection" Value="RightToLeft"/>
            </Style>
            <Style TargetType="ListBoxItem" BasedOn="{StaticResource {x:Type ListBoxItem}}">
                <Setter Property="FontFamily" Value="Noto Kufi Arabic, Segoe UI"/>
            </Style>
            <Style TargetType="MenuItem" BasedOn="{StaticResource {x:Type MenuItem}}">
                <Setter Property="FontFamily" Value="Noto Kufi Arabic, Segoe UI"/>
            </Style>
            <Style TargetType="Label" BasedOn="{StaticResource {x:Type Label}}">
                <Setter Property="FontFamily" Value="Noto Kufi Arabic, Segoe UI"/>
            </Style>
            <Style TargetType="PasswordBox" BasedOn="{StaticResource {x:Type PasswordBox}}">
                <Setter Property="FontFamily"    Value="Noto Kufi Arabic, Segoe UI"/>
                <Setter Property="FlowDirection" Value="RightToLeft"/>
            </Style>
            <Style TargetType="Window">
                <Setter Property="FontFamily" Value="Noto Kufi Arabic, Segoe UI"/>
            </Style>

            <!-- ═══════════════════════════════════════════
                 DATA GRID  —  global base
            ═══════════════════════════════════════════ -->
            <Style TargetType="DataGrid" BasedOn="{StaticResource {x:Type DataGrid}}">
                <Setter Property="FontFamily"                Value="Noto Kufi Arabic, Segoe UI"/>
                <Setter Property="FlowDirection"             Value="RightToLeft"/>
                <Setter Property="Background"                Value="{StaticResource SurfaceWhite}"/>
                <Setter Property="BorderBrush"               Value="{StaticResource BorderLight}"/>
                <Setter Property="BorderThickness"           Value="1"/>
                <Setter Property="RowBackground"             Value="{StaticResource SurfaceWhite}"/>
                <Setter Property="AlternatingRowBackground"  Value="#f9fbfa"/>
                <Setter Property="GridLinesVisibility"       Value="Horizontal"/>
                <Setter Property="HorizontalGridLinesBrush"  Value="#eaeff0"/>
                <Setter Property="SelectionMode"             Value="Single"/>
                <Setter Property="CanUserAddRows"            Value="False"/>
                <Setter Property="CanUserDeleteRows"         Value="False"/>
                <Setter Property="AutoGenerateColumns"       Value="False"/>
                <Setter Property="ColumnHeaderHeight"        Value="42"/>
                <Setter Property="RowHeight"                 Value="38"/>
            </Style>

            <!-- ── Column Header ── -->
            <Style TargetType="DataGridColumnHeader"
                   BasedOn="{StaticResource {x:Type DataGridColumnHeader}}">
                <Setter Property="FontFamily"               Value="Noto Kufi Arabic, Segoe UI"/>
                <Setter Property="FontSize"                 Value="13"/>
                <Setter Property="FontWeight"               Value="SemiBold"/>
                <Setter Property="Foreground"               Value="{StaticResource Primary}"/>
                <Setter Property="Background"               Value="{StaticResource SurfaceGrey}"/>
                <Setter Property="BorderBrush"              Value="{StaticResource BorderLight}"/>
                <Setter Property="BorderThickness"          Value="0,0,0,2"/>
                <Setter Property="Padding"                  Value="14,0,14,0"/>
                <Setter Property="MinHeight"                Value="42"/>
                <Setter Property="HorizontalContentAlignment" Value="Left"/>
                <Setter Property="FlowDirection"            Value="RightToLeft"/>
                <Setter Property="Cursor"                   Value="Hand"/>
                <Style.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="Background" Value="{StaticResource PrimaryGhost}"/>
                        <Setter Property="Foreground" Value="{StaticResource PrimaryDark}"/>
                    </Trigger>
                    <Trigger Property="SortDirection" Value="Ascending">
                        <Setter Property="Background" Value="{StaticResource PrimaryGhost}"/>
                    </Trigger>
                    <Trigger Property="SortDirection" Value="Descending">
                        <Setter Property="Background" Value="{StaticResource PrimaryGhost}"/>
                    </Trigger>
                </Style.Triggers>
            </Style>

            <!-- ── Row ── -->
            <Style TargetType="DataGridRow"
                   BasedOn="{StaticResource {x:Type DataGridRow}}">
                <Setter Property="BorderThickness" Value="0"/>
                <Style.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="Background" Value="#f0faf6"/>
                    </Trigger>
                    <Trigger Property="IsSelected" Value="True">
                        <Setter Property="Background" Value="#d4efe7"/>
                        <Setter Property="Foreground" Value="{StaticResource PrimaryDark}"/>
                    </Trigger>
                </Style.Triggers>
            </Style>

            <!-- ── Cell ── -->
            <Style TargetType="DataGridCell"
                   BasedOn="{StaticResource {x:Type DataGridCell}}">
                <Setter Property="BorderThickness"          Value="0"/>
                <Setter Property="Padding"                  Value="14,0"/>
                <Setter Property="VerticalAlignment"        Value="Center"/>
                <Setter Property="HorizontalAlignment"      Value="Stretch"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="DataGridCell">
                            <Border Padding="14,8"
                                    Background="{TemplateBinding Background}"
                                    BorderThickness="0">
                                <ContentPresenter VerticalAlignment="Center"
                                                  HorizontalAlignment="Left"/>
                            </Border>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
                <Style.Triggers>
                    <Trigger Property="IsSelected" Value="True">
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="BorderThickness" Value="0"/>
                    </Trigger>
                    <Trigger Property="IsFocused" Value="True">
                        <Setter Property="BorderThickness" Value="0"/>
                    </Trigger>
                </Style.Triggers>
            </Style>

            <!-- ── Row Header (hide the left gutter) ── -->
            <Style TargetType="DataGridRowHeader">
                <Setter Property="Width" Value="0"/>
                <Setter Property="Visibility" Value="Collapsed"/>
            </Style>

        </ResourceDictionary>
    </Application.Resources>
</Application>
